streamlit==1.8.0  # Example version, replace with the one you use
pandas==1.3.3  # Example version, replace with the one you use
Pillow==8.3.1  # This is for PIL, replace with the version you use
pytube==11.0.1  # Example version, replace with the one you use
pydub==0.25.1  # Example version, replace with the one you use
SpeechRecognition==3.8.1  # This is for speech_recognition, replace with the version you use
scikit-image==0.18.3  # This is for skimage, replace with the version you use
anthropic==0.21.3# anthropic - Special case: Include the correct package name and version if available
google-generativeai==0.4.1# google-generativeai - Special case: Include the correct package name and version if available
base64==1.0.0  # Likely doesn't need to be included as it's part of the standard library, listed here just in case
